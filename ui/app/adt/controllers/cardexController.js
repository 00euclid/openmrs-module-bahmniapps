'use strict';

angular.module('bahmni.adt')
    .controller('CardexController', [
        '$scope', '$rootScope', '$stateParams', 'spinner',
        ($scope, $rootScope, $stateParams, spinner) => {
            const init = () => {
                
            };

            init();
        }
    ]);